# Railway Environment Variables
# Copy these to your Railway project settings

# Port (Railway sets this automatically)
PORT=3000

# Node Environment
NODE_ENV=production

# Client URL for CORS (optional - defaults to allow all)
# CLIENT_URL=https://your-railway-app.up.railway.app
